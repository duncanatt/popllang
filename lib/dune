(library
 (name popllang)
 (modules math expr ast parser lexer))

(menhir (modules parser))
(ocamllex lexer)

; (include_subdirs qualified)

; (subdir
;  Langone
;  (menhir (modules parser))
;  (ocamllex lexer))